# Agent Jo Jobs — only what the job search needs.
# The main Agent Jo app needs far more; this is a subset on purpose.

anthropic>=0.40
openai>=1.50
httpx>=0.27,<0.28
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pypdf>=4.2
python-docx>=1.1
beautifulsoup4>=4.12
cryptography>=42.0

# optional — portal applications need a real browser:
#   pip install playwright && python -m playwright install chromium
